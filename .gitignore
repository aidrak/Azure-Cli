# ============================================================================
# AVD Configuration Files (User-Specific, Never Commit)
# ============================================================================
# These files contain sensitive information like subscription IDs, tenant IDs,
# and custom resource naming. They should NEVER be committed to git.

# Configuration files (never commit)
config/avd-config.sh
config/avd-config.ps1
config/*-config.sh
config/*-config.ps1

# Secrets file (NEVER commit - contains passwords and sensitive data)
secrets.yaml

# User-specific configuration (keep only .example files)
config.yaml

# Keep only example files (these are safe to commit)
!config/*.example.sh
!config/*.example.ps1
!config.yaml.example
!secrets.yaml.example

# ============================================================================
# Azure Credentials & Authentication
# ============================================================================
.azure/
.azureprofile
*.publishsettings
*.jks

# ============================================================================
# IDE & Editor Files
# ============================================================================
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db
*.sublime-project
*.sublime-workspace

# ============================================================================
# Temporary & Log Files
# ============================================================================
*.tmp
*.log
*.txt
*.pid
deployment.log
error.log

# ============================================================================
# OS Specific
# ============================================================================
.DS_Store
Thumbs.db
ehthumbs.db
$RECYCLE.BIN/

# ============================================================================
# Build Artifacts
# ============================================================================
*.zip
*.tar.gz
*.7z
dist/
build/
out/

# ============================================================================
# PowerShell Specific
# ============================================================================
*.ps1xml
*.psd1
*.psm1
*.pssc

# ============================================================================
# Python
# ============================================================================
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
.venv

# ============================================================================
# Node.js
# ============================================================================
node_modules/
npm-debug.log
package-lock.json

# ============================================================================
# Terraform (if used)
# ============================================================================
*.tfstate
*.tfstate.*
.terraform/
.terraform.lock.hcl

# ============================================================================
# Deployment State & Runtime Files (Instance-Specific, Never Commit)
# ============================================================================
# These files contain deployment state and runtime data specific to each
# Azure instance. They should be regenerated on each clone.

# State files (contain instance-specific deployment state)
state.json
state.db

# Discovery outputs (instance-specific runtime data)
discovered/

# Runtime artifacts (logs, temporary files, workflow tracking)
artifacts/logs/
artifacts/temp/
artifacts/workflow-logs/
artifacts/workflow-state/
artifacts/*.jsonl
artifacts/*.json

# Keep artifacts directory structure
!artifacts/.gitkeep
!artifacts/logs/.gitkeep
!artifacts/temp/.gitkeep
!artifacts/workflow-logs/.gitkeep
!artifacts/workflow-state/.gitkeep

# ============================================================================
# Archived/Legacy Code (Not Part of Current System)
# ============================================================================
# Legacy modules have been superseded by the YAML-based capability system
legacy/
tests/legacy/
scripts/archive-legacy-modules.sh

# Test artifacts (runtime-generated)
tests/logs/
